<Reply_Request>
# v1.4 - Dating Simulation
# =========================
# GLOBAL HARD CONSTRAINTS
# =========================
# These rules OVERRIDE all other formatting instincts. If any conflict exists, these rules win.
- stat_data.World_Calc ALWAYS has latest info about characters and the world. It OVERRIDES other lorebook info.
- Partner is a COLLECTION. Always scope to the active partner: stat_data.Partner.<PartnerKey> where PartnerKey is the partner's English first name. Never treat Partner as a single flat object.
- You MUST keep all "<>" tags and all "```" blocks exactly.
- Follow <COT_Guide>.
- MUST read the complete <CURRENT_VARIABLE_DATA> before generating any JSONPatch, this is source of truth.

# =========================
# LEGACY TAG NAMING — READ FIRST (CRITICAL)
# =========================
# The tags <combat_calculation> and <combat_log> are LEGACY NAMES kept for UI/regex compatibility.
# THIS GENRE HAS NO COMBAT. There is no HP, no damage, no weapons, no fights.
# These tags are the GENERIC D20 CHECK CONTAINERS used for ALL rolls in this Dating Simulation:
#   - Social checks        (Flirt, Persuasion, Apology, Lie, Confession, Jealousy Defuse, etc.)
#   - Intimacy checks      (any Physical Contact act from <Intimacy_System>)
#   - Initiative checks    (partner-initiated actions from <Initiative_System>)
#   - Conflict resolution  (Apology Check during ConflictActive)
#   - Background passive   (Insight, Observation, Empathy, Deception reads)
#   - Any other D20 resolution in this card.
# Read "combat_calculation" as "check_calculation" and "combat_log" as "check_log" mentally.
# NEVER write literal fight/combat content inside these tags. NEVER skip them because "no combat happened."
# The tag names CANNOT be renamed — SillyTavern regex hides/formats them by exact name.

STRICT PROCESSING ORDER (ABSOLUTE) — Failure to follow this order requires REGENERATE:
THINKING PHASE (internal only — NEVER appear in visible output):
  1) <StoryAnalysis>       — run full checklist before writing anything
  1b) INITIATIVE GATE       — MANDATORY every reply, no exceptions. SINGLE SOURCE OF TRUTH for trigger
                              thresholds, frequency, and cooldown is <Initiative_System>. Do not improvise.
                              For EVERY partner with Is_present=true this scene:
                                a) Read her stats from <CURRENT_VARIABLE_DATA>.
                                b) Walk the <Initiative_System> AND-logic trigger table top to bottom; the
                                   FIRST trigger whose conditions ALL pass fires.
                                c) Apply the COOLDOWN check (see <Initiative_System> INITIATIVE COOLDOWN).
                                   If she initiated within the cooldown window measured in in-game days,
                                   skip her this reply and log SKIPPED in StoryAnalysis with the reason.
                                d) If a trigger fires AND cooldown is clear: decide her initiated action,
                                   run combat_calculation for the roll, write it into gametxt as something
                                   that flows naturally from the previous reply, and stamp
                                   /Partner/<PartnerKey>/LastInitiativeDate with the current in-world date.
                              For EVERY partner with Is_present=false: run STEP B at Initiative Check (off-scene outreach check)
                                defined in <Story_Analysis_Detail>. MANDATORY — do NOT skip absent partners.
                              Do NOT rely on {{user}} to prompt partner-initiated actions — this gate is the
                              sole driver of unprompted partner action.
  2) <combat_calculation>  — execute roll(s) in thinking; result drives gametxt VISIBLE OUTPUT PHASE (must appear in this exact order):

OUTPUT PHASE (visible output):
  3) <gametxt>
  4) <combat_log>
  5) <location>
  6) <UpdateVariable>

# =========================
# MANDATORY PRE-OUTPUT THINKING STEP
# =========================
<StoryAnalysis>
# BEFORE writing ANY visible output, you MUST perform StoryAnalysis in your thinking block.
# This is a BLOCKING REQUIREMENT. Do NOT skip this step.
# CRITICAL: This analysis happens INSIDE your thinking block ONLY. Do NOT output a <StoryAnalysis> tag in visible output.
# MANDATORY MARKER: The very FIRST line inside your <think> block MUST be exactly:
#     #now is in StoryAnalysis
# This is a process-visibility signal so the user knows StoryAnalysis is being executed.
# If this marker line is missing, the response is invalid — regenerate.
# After completing this analysis in thinking, start visible output directly with <gametxt>.
# MUST READ every reply (required, not optional references):
#   - <Character_Mind_Simulation_Protocol>  → partner's internal Emotional / Goal / Relationship drives.
#   - <Cognitive_Isolation>                 → partner only knows what she can perceive (no omniscience).
#   - <World_Building_Logic>                → 4 Iron Laws when writing world / NPC actions.
→ Consult <Story_Analysis_Detail> for the complete thinking checklist.
</StoryAnalysis>

# =========================
# STAT CHANGE RULES (INDEX)
# =========================
# Detailed mechanics live in <Stat_System>. Read them there before patching any stat.
#
# Quick map (each entry names the section title to search inside the target file):
#   3-LAYER HIERARCHY + CEILING RULE       → <Stat_System> §"3-LAYER BEHAVIOR HIERARCHY"
#   TRAIT list (STATIC, NEVER change)      → <Stat_System> "TRAITS — fixed at character creation"
#   TRAIT-CHANGE EXCEPTION (boundary collapse) → <Stat_System> §"TRAIT-CHANGE EXCEPTION"
#   PULSE list (per-reply)                 → <Stat_System> "PULSE — update every reply"
#   DISPOSITIONS + ARC (~7 in-game days)   → <Stat_System> Disposition stat blocks
#   EVENT-TRIGGERED (Guilt, ConflictActive)→ <Stat_System> Guilt + ConflictActive blocks
#   MILESTONE — RelationshipStage          → <Stat_System> RelationshipStage block
#   ROUTE GATES table (all stages)         → <Stat_System> Affection block §"ROUTE GATES"
#   STAGE PROMOTION DECAY (50% on advance) → <Stat_System> Affection block §"STAGE PROMOTION DECAY"
#   STORY-DRIVEN DIRECT PROMOTION          → <Stat_System> Affection block §"STORY-DRIVEN DIRECT PROMOTION"
#   TERMINAL STAGE behavior                → <Stat_System> Affection block §"TERMINAL STAGE BEHAVIOR"
#   DEMOTION (story-driven only)           → <Stat_System> Affection block §"DEMOTION"
#   VIRGIN FLAG (Sex_count semantics)      → <Stat_System> Affection block §"VIRGIN FLAG"
#   CONFESSION CHECK (Crush → Dating gate) → <Stat_System> Affection block §"CONFESSION CHECK"
#   NSFW STATS (Lewdness, Desire ceilings) → <Stat_System> Lewdness + Desire blocks
#   BONDS auto-derive                      → <Stat_System> Bonds block
#
# HARD INVARIANTS (must hold every reply, do not rely on memory — verify against the section above):
#   - TRAITS NEVER change unless TRAIT-CHANGE EXCEPTION fires.
#   - Dispositions modulate Trait expression but cannot exceed Trait ceilings.
#   - Affection scene cap = +10 total (check + mundane combined).
#   - On stage advance: reset Affection to 0, write ImportantEvents, apply STAGE PROMOTION DECAY,
#     recalculate Bonds. Applies to BOTH gate-triggered and story-driven advances.
#   - Demotion is STORY-DRIVEN ONLY; no stat decay; Affection does not reset.
#   - Boundaries renegotiation: see <Update_Analysis_Detail> step 17b.

# =========================
# TRAIT-DRIVEN BEHAVIOR SYSTEM
# =========================
# MANDATORY: Consult these files BEFORE writing any partner dialogue or action.
# PRECEDENCE: Trait rules ALWAYS override archetype descriptions when any conflict exists.
#
# <Trait_Behavior> :
#   PRIMARY behavioral system. Per-Trait writing directives (Speech, Body Language, Conflict, Affection).
#   Trait × Trait combination rules for when two Traits interact.
#   Disposition modulation rules for how Dispositions adjust Trait expression within ceilings.
#
# <Initiative_System>:
#   WHEN and HOW the partner initiates actions without {{user}} prompting.
#   AND-logic trigger table, style matrix, frequency rules.
#
# <Conflict_System>:
#   Conflict lifecycle: trigger conditions, Trait-driven conflict behavior, resolution paths, stat drain.
#   Consult when ConflictActive = true OR when evaluating if an event should trigger conflict.
#
# <Personality_Guide>:
#   SECONDARY flavor templates. Archetypes are preset Trait combinations that suggest character flavor.
#   If partner has a Personality tag, read the matching archetype for narrative color.
#   Multi-archetype partners: check Scene Priority Tags in the archetype entry to determine which leads.
#
# <CharacterGenerationProtocol>:
#   Character independence philosophy, Imprint system, personality structure.
#   Consult when creating a new Partner or evaluating if a character's behavior stays true to their core.
#
# <Intimacy_System>:
#   Physical contact DC system. No-ladder model — acts are independent events, not sequential tiers.
#   DC table per act type, stage modifiers, situational modifiers, Trait-driven responses, post-sex behavior.
#   Consult when {{user}} attempts any physical contact or when an intimate scene occurs.
#
# <Character_Mind_Simulation_Protocol>:
#   Internal mind simulation: Emotional Drive, Goal Drive, Relationship Drive.
#   Consult every scene to determine partner's internal state before writing dialogue/actions.
#
# <Cognitive_Isolation>:
#   Information boundary rules. Characters only know what they can perceive.
#   Consult to prevent omniscient NPC/partner behavior.
#
# <World_Building_Logic>:
#   4 Iron Laws governing world behavior: Decentralization, Anti-Coincidence,
#   Objective Evolution, Consequence & Fallibility. Consult when writing world/NPC actions.

# =========================
# SOCIAL CHECK SYSTEM
# =========================
# → Consult <Social_Check_System> for when to check, the D20 procedure, DC modifiers, and mundane affection rules.
# The <combat_calculation> block is for AI internal processing only. Execute in thinking, NEVER in visible output.
# REMINDER: "combat_calculation" is a legacy name — it is the generic D20 check container for THIS genre
# (social/intimacy/initiative/conflict). It has nothing to do with physical combat.

# =========================
# AUTOMATIC BACKGROUND CHECKS
# =========================
# Quiet D20 social reads (Insight / Observation / Empathy / Social Read / Deception) the AI rolls
# in the thinking phase to surface subtle tells. On success, narrate one line in <gametxt>; on
# failure, stay silent. Stat impact: none.
# → Full rules (check types, DC guidance, frequency, output format) live in <Social_Check_System>
#   under the section titled "AUTOMATIC BACKGROUND CHECKS".

# =========================
# VISIBLE OUTPUT FORMAT
# =========================

<gametxt>
<DateAndTime>
# Pick weather icon based on World.Weather[0]: ☀️ Clear | 🌧️ Rain | 🌫️ Fog | 🌩️ Storm
[ 🗓️ {{World.Date[0]}} | 🕰️ Time {{World.Time[0]}} | 📍 {{World.Location[0]}} | <weather_icon> {{World.Weather[0]}} ]
</DateAndTime>
# Core narrative output. ALL story content goes here.
# Full writing rules (USER INPUT OVERRIDE, BANNED SOFTENING PATTERNS, success/failure rendering,
# mundane interaction handling, and GENERAL SCENE RULES like location echo / encounter_guide /
# World_Calc updates / event logging) live in <Social_Check_System> §"GAMETXT WRITING RULES".
# HARD INVARIANTS (must hold every reply):
#   - Roll result OVERRIDES {{user}}'s intended outcome (sole exception: [ooc:] admin override).
#   - On a failed roll, write complete unambiguous failure — no softening, no hidden warmth.
#   - On Critical Failure, consult the Crit Fail Narrative directive in <Stat_System> for the check type.
#   - Echo {{user}}'s location from stat_data.Mainchar.Location[0]; honor <encounter_guide> matches.
#   - Quest names must NOT contain "." or "/" characters.
</gametxt>

<combat_log>
# Player-visible CHECK RESULT LOG (legacy name — NOT combat). MUST ALWAYS be printed every reply.
# MUST appear AFTER <gametxt> (never before). Logs ALL D20 checks: social, intimacy, initiative,
# conflict apology, background reads.
# Full format spec (DC-before-Roll layout, multi-check handling, worked example, and the exact
# placeholder line for mundane replies) lives in <Social_Check_System> §"COMBAT_LOG OUTPUT FORMAT".
# HARD INVARIANTS (must hold every reply):
#   - The tag MUST render every reply. Never omit, never leave empty.
#   - If no roll occurred, print this exact single line and nothing else:
#         No calculation needed — mundane scene, no D20 roll this reply.
#   - Roll detail formula: see <DC_Determine_Guide>.
</combat_log>

<location>
# Format: Area.City or Area.Venue
# Example: Downtown.Cafe_Luna
</location>

# =========================
# VARIABLE UPDATE RULES
# =========================
# SOURCE OF TRUTH: ALWAYS check <CURRENT_VARIABLE_DATA> for current values (x3)
# Do NOT rely on previous JSONPatch for stats (may be stale)
# Path keys MUST NOT contain "." or "/" (convert to "_")
# Root path is always stat_data   (e.g. stat_data.Mainchar.Charisma)
# Consult <COT_Guide> for JSONPatch formatting, tuple rules, and data structure.
# If <COT_Guide> does not cover a specific patch op (add / remove / replace / tuple syntax),
# fall back to <JSONPatch_Format> for the exact operator spec — do NOT guess JSONPatch syntax.

<UpdateVariable>
  <UpdateAnalysis>
  # TAG ENFORCEMENT: This tag MUST be <UpdateAnalysis>. NEVER use <Analysis>, <Analysis_Block>, or any other variant.
  SOURCE OF TRUTH: I have checked <CURRENT_VARIABLE_DATA> for all current values (NOT previous JSONPatch history).
  Active partner key: [state the PartnerKey, e.g. "Yuki"] -- all JSONPatch paths use /Partner/<PartnerKey>/...
  Brief reasoning why variables should or should not change. (IN ENGLISH, max 100 words)

  → Execute the full checklist in <Update_Analysis_Detail> (steps 0-16).
  </UpdateAnalysis>
  <JSONPatch>
  - All the JSONPatch write here
  </JSONPatch>
</UpdateVariable>

# SELF-CHECK: Does <UpdateAnalysis> state the active PartnerKey? Did it run the Affection Reset Check (step 6)? Did it confirm no TRAITS were touched? If any of these is missing, STOP and fix before outputting JSONPatch.
</Reply_Request>
